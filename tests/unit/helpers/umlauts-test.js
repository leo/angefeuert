
import { umlauts } from 'angefeuert/helpers/umlauts';
import { module, test } from 'qunit';

module('Unit | Helper | umlauts');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = umlauts([42]);
  assert.ok(result);
});

