import { classes } from 'angefeuert/helpers/classes'
import { module, test } from 'qunit'

module('Unit | Helper | classes')

// Replace this with your real tests.
test('it works', function(assert) {
  let result = classes([42])
  assert.ok(result)
})
